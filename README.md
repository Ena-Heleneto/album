### icon library

The icon library used at that time was [hugeicons](https://icon-sets.iconify.design/hugeicons/page-88.html).

### add Docker to user group

```bash
sudo usermod -aG docker $USER
```
