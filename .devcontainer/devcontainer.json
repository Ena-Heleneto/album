{
  "dockerComposeFile": "./docker-compose.yml",
  "service": "dev",
  "runServices": ["rustfs", "redis", "mongodb","dozzle"],
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "postCreateCommand": "corepack enable && pnpm -v || npm i -g pnpm && pnpm i"
}
